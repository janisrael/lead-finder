apiVersion: v1
kind: Service
metadata:
  name: leadfinder-service
  namespace: leadfinder
  labels:
    app: leadfinder
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 5000
    protocol: TCP
    name: http
  selector:
    app: leadfinder

